# Pinned Raspberry Pi OS Lite (64-bit) release for qemu integration tests.
# Use the directory name from the mirror (raspios_lite_arm64-YYYY-MM-DD) or just
# the date (YYYY-MM-DD). Override: RPI_PRESEED_QEMU_PIOS_RELEASE=latest
RPI_PRESEED_QEMU_PIOS_RELEASE=2026-06-19

# Official image index (downloads.raspberrypi.com and .org are equivalent).
RPI_PRESEED_QEMU_PIOS_MIRROR=https://downloads.raspberrypi.com/raspios_lite_arm64/images
RPI_PRESEED_QEMU_PIOS_MIRROR_FALLBACK=https://downloads.raspberrypi.org/raspios_lite_arm64/images
